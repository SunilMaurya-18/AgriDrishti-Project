import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard',
  description: 'Monitor real-time soil sensors, weather, AI recommendations, and disease alerts from your farm dashboard.',
};

export { default } from './page';
